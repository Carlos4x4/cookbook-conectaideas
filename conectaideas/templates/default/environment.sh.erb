PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games"
<% node[:conectaideas][:environment].keys.each do |key| %>
<%= key %>=<%= node[:conectaideas][:environment][key] %> 
<% end %>