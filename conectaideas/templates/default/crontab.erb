PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games"
<% node[:deploy][:conectaideas][:environment].keys.each do |key| %>
<% if !node[:deploy][:conectaideas][:environment][key].nil? and node[:deploy][:conectaideas][:environment][key].size > 2%>
<%= key %>=<%= node[:deploy][:conectaideas][:environment][key] %>
<% end %>
<% end %>

*/10 * * * * deploy cd /srv/www/conectaideas/current && bundle exec rake sagde:cerrar_sesiones_alerta_inactivas > /dev/null 2> /dev/null
0 4 * * * deploy cd /srv/www/conectaideas/current && bundle exec rake sagde:backup > /dev/null 2> /dev/null
0 5 * * * deploy cd /srv/www/conectaideas/current && bundle exec rake sagde:diario > /dev/null 2> /dev/null
